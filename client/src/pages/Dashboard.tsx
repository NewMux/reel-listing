import { ArrowUpRight, Clapperboard, Clock3, Plus, Sparkles } from "lucide-react";
import { useLocation } from "wouter";
import { AppSidebar, StatusPill } from "@/components/AppChrome";
import { copy, useLocale } from "@/lib/locale";
import { trpc } from "@/lib/trpc";

const PROJECT_IMAGE = "/manus-storage/vistaflow-project-villa_cdc98c35.jpg";

export default function Dashboard() {
  const { locale } = useLocale(); const t = copy[locale]; const [, setLocation] = useLocation(); const projects = trpc.projects.list.useQuery();
  return <AppSidebar><main className="mx-auto max-w-[1320px] px-5 py-9 sm:px-8 lg:px-10 lg:py-12"><div className="flex flex-col gap-5 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-sm font-semibold text-[#7D6252]">{t.dashboard.greeting}</p><h1 className="serif mt-2 text-4xl tracking-[-.05em] sm:text-5xl">{t.dashboard.title}</h1><p className="mt-3 max-w-[510px] text-sm leading-6 text-[#766C66]">{t.dashboard.body}</p></div><button onClick={() => setLocation("/projects/new")} className="inline-flex h-11 items-center justify-center gap-2 rounded-xl bg-[#251811] px-4 text-sm font-bold text-white hover:bg-[#402E24]"><Plus size={16}/>{t.dashboard.newProject}</button></div><section className="mt-10"><div className="mb-4 flex items-center justify-between"><h2 className="text-xs font-bold uppercase tracking-[.15em] text-[#7D736D]">{t.dashboard.recent}</h2>{projects.data && projects.data.length > 0 && <span className="text-xs text-[#89807B]">{projects.data.length} {projects.data.length === 1 ? "project" : "projects"}</span>}</div>{projects.isLoading ? <div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{Array.from({length:3}).map((_,i)=><div key={i} className="h-72 animate-pulse rounded-[23px] bg-[#EDE3DD]"/>)}</div> : projects.data?.length ? <div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{projects.data.map(project => <article key={project.id} className="group overflow-hidden rounded-[23px] border border-[#251811]/9 bg-white shadow-[0_4px_18px_rgba(17,37,30,.03)] transition-all hover:-translate-y-1 hover:shadow-[0_14px_30px_rgba(17,37,30,.1)]"><div className="relative h-40 overflow-hidden bg-[#E7E1DD]">{project.mediaUrls[0] && project.mediaTypes[0]?.startsWith("image/") ? <img src={project.mediaUrls[0]} alt="" className="h-full w-full object-cover transition-transform duration-500 group-hover:scale-105"/> : <img src={PROJECT_IMAGE} alt="" className="h-full w-full object-cover"/>}<div className="absolute left-3 top-3"><StatusPill status={project.status}/></div></div><div className="p-4"><p className="truncate text-base font-bold text-[#36261C]">{project.title}</p><p className="mt-1 flex items-center gap-1.5 text-xs text-[#817975]"><Clock3 size={13}/>{project.location}</p><button onClick={() => setLocation(project.status === "Review" ? `/projects/${project.id}/review` : `/projects/${project.id}`)} className="mt-5 inline-flex items-center gap-1 text-sm font-bold text-[#61412F] hover:text-[#251811]">{t.dashboard.view}<ArrowUpRight size={15}/></button></div></article>)}</div> : <div className="overflow-hidden rounded-[27px] border border-dashed border-[#251811]/16 bg-[#F1E9E4] p-7 sm:p-10"><div className="grid h-11 w-11 place-items-center rounded-2xl bg-[#E9C6B2] text-[#6F3E22]"><Clapperboard size={20}/></div><h3 className="serif mt-6 text-3xl tracking-[-.04em]">{t.dashboard.emptyTitle}</h3><p className="mt-3 max-w-[520px] text-sm leading-6 text-[#736963]">{t.dashboard.emptyBody}</p><button onClick={() => setLocation("/projects/new")} className="mt-6 inline-flex h-11 items-center gap-2 rounded-xl bg-[#251811] px-4 text-sm font-bold text-white"><Sparkles size={16}/>{t.dashboard.emptyAction}</button></div>}</section><section className="mt-12 rounded-[22px] border border-[#251811]/8 bg-white/60 p-5"><p className="text-xs font-bold uppercase tracking-[.15em] text-[#7D736D]">{t.dashboard.statuses}</p><div className="mt-4 flex flex-wrap gap-2">{["Uploading","Processing","Review","Done"].map(status => <StatusPill key={status} status={status}/>)}</div></section></main></AppSidebar>;
}
