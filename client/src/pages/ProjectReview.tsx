import { ArrowLeft, Check, FileVideo, Loader2, MessageSquareText, Play, Sparkles } from "lucide-react";
import { useState } from "react";
import { useLocation, useRoute } from "wouter";
import { AppSidebar, StatusPill } from "@/components/AppChrome";
import { copy, useLocale } from "@/lib/locale";
import { trpc } from "@/lib/trpc";

export default function ProjectReview() {
  const { locale } = useLocale(); const t = copy[locale]; const [, params] = useRoute("/projects/:id/review"); const id = Number(params?.id); const [, setLocation] = useLocation(); const [notes, setNotes] = useState(""); const [showNotes, setShowNotes] = useState(false); const project = trpc.projects.get.useQuery({ id }, { enabled: Number.isSafeInteger(id) }); const utils = trpc.useUtils(); const approve = trpc.projects.approve.useMutation({ onSuccess: data => { utils.projects.get.invalidate({id}); utils.projects.list.invalidate(); if (data) setLocation(`/projects/${id}`); } }); const request = trpc.projects.requestChanges.useMutation({ onSuccess: () => { utils.projects.get.invalidate({id}); setNotes(""); setShowNotes(false); } });
  if (project.isLoading) return <AppSidebar><div className="p-10 text-sm text-[#65746B]">{t.common.loading}</div></AppSidebar>;
  if (!project.data) return <AppSidebar><div className="p-10 text-sm text-[#65746B]">Project not found.</div></AppSidebar>;
  const data = project.data;
  return <AppSidebar><main className="mx-auto max-w-[1220px] px-5 py-8 sm:px-8 lg:px-10 lg:py-12"><button onClick={() => setLocation("/dashboard")} className="inline-flex items-center gap-2 text-sm font-semibold text-[#63756A] hover:text-[#11251E]"><ArrowLeft size={16}/>{t.common.back}</button><div className="mt-8 grid gap-10 lg:grid-cols-[.9fr_1.1fr]"><div><div className="flex items-center gap-3"><p className="text-xs font-bold uppercase tracking-[.15em] text-[#6E8249]">{t.review.eyebrow}</p><StatusPill status={data.status}/></div><h1 className="serif mt-3 text-5xl leading-[.98] tracking-[-.05em]">{t.review.title}</h1><p className="mt-4 max-w-[480px] text-sm leading-7 text-[#63746A]">{t.review.body}</p><div className="mt-8 rounded-[22px] bg-[#EAF0DC] p-5"><div className="flex items-center gap-3"><span className="grid h-9 w-9 place-items-center rounded-xl bg-[#D8E9B2] text-[#436515]"><Sparkles size={17}/></span><div><p className="text-sm font-bold text-[#284633]">{data.title}</p><p className="mt-0.5 text-xs text-[#6A7B6E]">{data.location}</p></div></div></div></div><div className="rounded-[27px] border border-[#11251E]/10 bg-white p-5 shadow-[0_16px_40px_rgba(17,37,30,.05)] sm:p-7"><div className="flex items-center justify-between"><h2 className="text-sm font-bold">{t.review.media}</h2><span className="text-xs text-[#758178]">{data.mediaUrls.length} {data.mediaUrls.length === 1 ? "file" : "files"}</span></div><div className="mt-4 grid grid-cols-2 gap-3 sm:grid-cols-3">{data.mediaUrls.map((url, index) => <div key={url} className="relative overflow-hidden rounded-xl bg-[#E5E8DE]"><div className="h-28">{data.mediaTypes[index]?.startsWith("image/") ? <img src={url} className="h-full w-full object-cover" alt={`Property media ${index + 1}`}/> : <video controls preload="metadata" src={url} className="h-full w-full bg-[#10291F] object-cover" aria-label={`Property video ${index + 1}`}/>}</div><span className="absolute left-2 top-2 grid h-6 w-6 place-items-center rounded-full bg-[#11251E]/70 text-[10px] font-bold text-white">{index + 1}</span></div>)}</div>{data.revisionNotes && <div className="mt-5 rounded-xl bg-[#F7F1E7] p-3.5"><p className="text-[11px] font-bold uppercase tracking-[.1em] text-[#886534]">{t.review.changeSent}</p><p className="mt-1 text-sm leading-6 text-[#695C47]">{data.revisionNotes}</p></div>}{data.status === "Review" ? <><button disabled={approve.isPending} onClick={() => approve.mutate({ id })} className="mt-6 flex h-12 w-full items-center justify-center gap-2 rounded-xl bg-[#11251E] text-sm font-bold text-white disabled:opacity-70">{approve.isPending ? <Loader2 size={16} className="animate-spin"/> : <Check size={16}/>} {t.review.approve}</button><button onClick={() => setShowNotes(!showNotes)} className="mt-3 flex h-11 w-full items-center justify-center gap-2 rounded-xl border border-[#11251E]/12 text-sm font-bold text-[#355041] hover:bg-[#F5F6EF]"><MessageSquareText size={16}/>{t.review.request}</button>{showNotes && <div className="mt-4 rounded-xl border border-[#11251E]/10 bg-[#F8F7F1] p-3"><textarea value={notes} onChange={e => setNotes(e.target.value)} placeholder={t.review.notePlace} rows={3} className="w-full resize-none bg-transparent text-sm outline-none placeholder:text-[#9AA49D]"/><button disabled={notes.trim().length < 3 || request.isPending} onClick={() => request.mutate({id, notes})} className="mt-3 h-9 w-full rounded-lg bg-[#D8E9B2] text-xs font-bold text-[#35511A] disabled:opacity-50">{request.isPending ? t.common.loading : t.review.send}</button></div>}</> : <button onClick={() => setLocation(`/projects/${id}`)} className="mt-6 flex h-12 w-full items-center justify-center rounded-xl bg-[#11251E] text-sm font-bold text-white">View project</button>}</div></div></main></AppSidebar>;
}
