import { AlertCircle, ArrowLeft, FileVideo, Loader2, MapPin, UploadCloud, X } from "lucide-react";
import { ChangeEvent, DragEvent, useRef, useState } from "react";
import { useLocation } from "wouter";
import { AppSidebar } from "@/components/AppChrome";
import { copy, useLocale } from "@/lib/locale";
import { trpc } from "@/lib/trpc";

type LocalFile = { file: File; preview: string; kind: "image" | "video" };
const maxBytes = 25 * 1024 * 1024;

type UploadedMedia = { name: string; type: string; key: string; url: string };

function readChunkBase64(blob: Blob) {
  return new Promise<string>((resolve, reject) => {
    const reader = new FileReader();
    reader.onload = () => {
      if (typeof reader.result !== "string") return reject(new Error("Unable to read file chunk."));
      resolve(reader.result.split(",")[1] || "");
    };
    reader.onerror = () => reject(new Error("Unable to read file chunk."));
    reader.readAsDataURL(blob);
  });
}

export default function NewProject() {
  const { locale } = useLocale(); const t = copy[locale]; const [, setLocation] = useLocation(); const [files, setFiles] = useState<LocalFile[]>([]); const [title, setTitle] = useState(""); const [description, setDescription] = useState(""); const [location, setLocationValue] = useState(""); const [error, setError] = useState(""); const [dragging, setDragging] = useState(false); const [uploadProgress, setUploadProgress] = useState<number | null>(null); const inputRef = useRef<HTMLInputElement>(null); const startUpload = trpc.media.createUploadSession.useMutation(); const appendChunk = trpc.media.appendUploadChunk.useMutation(); const finalizeUpload = trpc.media.finalizeUploadSession.useMutation(); const create = trpc.projects.create.useMutation({ onSuccess: data => { setUploadProgress(100); setLocation(`/projects/${data.id}/review`); }, onError: err => { setError(err.message); setUploadProgress(null); } });
  const addFiles = (next: File[]) => { setError(""); const accepted = next.filter(file => ["image/jpeg","image/png","image/webp","video/mp4","video/quicktime"].includes(file.type)); if (accepted.length !== next.length) return setError("Use JPG, PNG, WEBP, MP4, or MOV media files."); const nextIsVideo = accepted.some(file => file.type.startsWith("video/")); const currentIsVideo = files.some(file => file.kind === "video"); if ((nextIsVideo && (accepted.length !== 1 || files.length)) || (currentIsVideo && accepted.length)) return setError("Upload either one walkthrough video or a set of property images, not both."); const merged = [...files, ...accepted.map(file => ({ file, preview: URL.createObjectURL(file), kind: file.type.startsWith("video/") ? "video" as const : "image" as const }))]; if (merged.length > 10) return setError("You can upload a maximum of 10 images."); if (merged.reduce((sum, item) => sum + item.file.size, 0) > maxBytes) return setError("Keep the combined upload size under 25 MB."); setFiles(merged); };
  const browse = (event: ChangeEvent<HTMLInputElement>) => event.target.files && addFiles(Array.from(event.target.files));
  const drop = (event: DragEvent<HTMLDivElement>) => { event.preventDefault(); setDragging(false); addFiles(Array.from(event.dataTransfer.files)); };
  const submit = async () => { setError(""); if (!title.trim() || !location.trim()) return setError("Add a property title and location to continue."); if (files.length === 0) return setError("Add property media to continue."); if (files[0].kind === "image" && files.length < 3) return setError("Add at least 3 property images to continue."); try { setUploadProgress(0); const totalBytes = files.reduce((sum, item) => sum + item.file.size, 0); let completeBytes = 0; const uploads: UploadedMedia[] = []; for (const item of files) { const session = await startUpload.mutateAsync({ name: item.file.name, type: item.file.type, totalBytes: item.file.size }); for (let offset = 0; offset < item.file.size; offset += session.chunkSize) { const chunk = await readChunkBase64(item.file.slice(offset, Math.min(offset + session.chunkSize, item.file.size))); const progress = await appendChunk.mutateAsync({ uploadId: session.id, chunk }); setUploadProgress(Math.min(88, Math.round(((completeBytes + progress.receivedBytes) / totalBytes) * 88))); } uploads.push(await finalizeUpload.mutateAsync({ uploadId: session.id })); completeBytes += item.file.size; } setUploadProgress(92); create.mutate({ title: title.trim(), description: description.trim() || undefined, location: location.trim(), files: uploads }); } catch (uploadError) { setError(uploadError instanceof Error ? uploadError.message : "We could not secure this media. Please try again."); setUploadProgress(null); } };
  return <AppSidebar><main className="mx-auto max-w-[1120px] px-5 py-8 sm:px-8 lg:px-10 lg:py-12"><button onClick={() => setLocation("/dashboard")} className="inline-flex items-center gap-2 text-sm font-semibold text-[#63756A] hover:text-[#11251E]"><ArrowLeft size={16}/>{t.common.back}</button><div className="mt-8 grid gap-10 lg:grid-cols-[.78fr_1.22fr]"><div><p className="text-xs font-bold uppercase tracking-[.15em] text-[#6E8249]">{t.upload.eyebrow}</p><h1 className="serif mt-3 text-5xl tracking-[-.05em]">{t.upload.title}</h1><p className="mt-4 max-w-sm text-sm leading-7 text-[#64756B]">{t.upload.body}</p><div className="mt-9 hidden rounded-[22px] bg-[#EAEFDE] p-5 lg:block"><p className="text-sm font-bold text-[#31503B]">A precise beginning</p><p className="mt-2 text-sm leading-6 text-[#6B7C70]">Your media is stored securely, then held for your review before AI generation can begin.</p></div></div><div className="rounded-[27px] border border-[#11251E]/10 bg-white p-5 shadow-[0_16px_40px_rgba(17,37,30,.05)] sm:p-7"><label className="text-sm font-bold text-[#254034]">{t.upload.media}</label><p className="mt-1 text-xs leading-5 text-[#758178]">{t.upload.mediaHint}</p><div onDragOver={e => { e.preventDefault(); setDragging(true); }} onDragLeave={() => setDragging(false)} onDrop={drop} onClick={() => inputRef.current?.click()} className={`mt-4 grid min-h-40 cursor-pointer place-items-center rounded-2xl border border-dashed p-5 text-center transition ${dragging ? "border-[#5E8535] bg-[#EAF2D7]" : "border-[#A7B4AA] bg-[#F8F7F2] hover:border-[#668647] hover:bg-[#F2F5E7]"}`}><input ref={inputRef} onChange={browse} type="file" accept="image/jpeg,image/png,image/webp,video/mp4,video/quicktime" multiple className="hidden"/><div><span className="mx-auto grid h-10 w-10 place-items-center rounded-xl bg-[#D8E9B2] text-[#456A26]"><UploadCloud size={19}/></span><p className="mt-3 text-sm font-bold text-[#2A4437]">{t.upload.drop}</p><p className="mt-1 text-xs text-[#758178]">{t.upload.browse}</p></div></div>{files.length > 0 && <div className="mt-4 grid grid-cols-2 gap-3 sm:grid-cols-3">{files.map((item, index) => <div key={`${item.file.name}-${index}`} className="group relative overflow-hidden rounded-xl border border-[#11251E]/10 bg-[#F1F0E9]"><div className="h-24">{item.kind === "image" ? <img src={item.preview} className="h-full w-full object-cover" alt="Property upload preview"/> : <div className="grid h-full place-items-center bg-[#DDE7D8] text-[#3B6335]"><FileVideo size={24}/></div>}</div><p className="truncate px-2 py-2 text-[10px] font-semibold text-[#56665D]">{item.file.name}</p><button onClick={event => { event.stopPropagation(); URL.revokeObjectURL(item.preview); setFiles(current => current.filter((_, i) => i !== index)); }} className="absolute right-1 top-1 grid h-6 w-6 place-items-center rounded-full bg-[#11251E]/75 text-white opacity-0 transition-opacity group-hover:opacity-100" aria-label={t.upload.remove}><X size={13}/></button></div>)}</div>}{uploadProgress !== null && <div className="mt-4 rounded-xl bg-[#EFF4E6] p-3"><div className="flex items-center justify-between text-xs font-bold text-[#496A38]"><span>{t.upload.processing}</span><span>{uploadProgress}%</span></div><div className="mt-2 h-1.5 overflow-hidden rounded-full bg-[#D8E6CC]"><div className="h-full rounded-full bg-[#668D3A] transition-all duration-200" style={{ width: `${uploadProgress}%` }}/></div></div>}<div className="mt-7 grid gap-5"><label className="grid gap-2 text-sm font-bold text-[#254034]">{t.upload.titleField}<input value={title} onChange={e => setTitle(e.target.value)} placeholder={t.upload.titlePlace} className="h-11 rounded-xl border border-[#11251E]/12 bg-[#FCFCFA] px-3 text-sm font-normal outline-none focus:border-[#5E8535] focus:ring-2 focus:ring-[#D8E9B2]"/></label><label className="grid gap-2 text-sm font-bold text-[#254034]">{t.upload.description}<textarea value={description} onChange={e => setDescription(e.target.value)} placeholder={t.upload.descriptionPlace} rows={3} className="rounded-xl border border-[#11251E]/12 bg-[#FCFCFA] px-3 py-2.5 text-sm font-normal outline-none focus:border-[#5E8535] focus:ring-2 focus:ring-[#D8E9B2]"/></label><label className="grid gap-2 text-sm font-bold text-[#254034]">{t.upload.location}<span className="relative"><MapPin size={16} className="absolute start-3 top-1/2 -translate-y-1/2 text-[#6D806E]"/><input value={location} onChange={e => setLocationValue(e.target.value)} placeholder={t.upload.locationPlace} className="h-11 w-full rounded-xl border border-[#11251E]/12 bg-[#FCFCFA] pe-3 ps-9 text-sm font-normal outline-none focus:border-[#5E8535] focus:ring-2 focus:ring-[#D8E9B2]"/></span></label></div>{error && <p className="mt-4 flex gap-2 rounded-xl bg-[#FFF1E5] px-3 py-2.5 text-xs font-medium text-[#94572C]"><AlertCircle size={15} className="shrink-0"/>{error}</p>}<button disabled={create.isPending || uploadProgress !== null} onClick={submit} className="mt-6 flex h-12 w-full items-center justify-center gap-2 rounded-xl bg-[#11251E] text-sm font-bold text-white disabled:opacity-70">{create.isPending || startUpload.isPending || appendChunk.isPending || finalizeUpload.isPending ? <Loader2 size={16} className="animate-spin"/> : null}{create.isPending || startUpload.isPending || appendChunk.isPending || finalizeUpload.isPending ? t.upload.processing : t.upload.continue}</button></div></div></main></AppSidebar>;
}
